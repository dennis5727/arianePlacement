"""Phase 4 (text-only) / Phase 5 (+ image):
   Builds the prompt, makes 1 API call per iteration,
   parses JSON {macro_id: region} response.
   Handles bad JSON / API errors with retry + fallback."""
# TODO: implement in Phase 4, extend in Phase 5
